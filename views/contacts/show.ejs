<!DOCTYPE html>
<html>
    <head>
        <%- include('../partials/head') %>
    </head>
    <body>
        <%- include('../partials/nav') %>

        <div class="container contact contact-show">
            <h2>Show</h2>
            <div class="card">
                <div class="card-body">
                    <h3 class="card-title"><%= contact.name %></h3>
                    <label>Email</label><span><%= contact.email %></span>
                    <bR>
                    <label>Phone</label><span><%= contact.phone %></span>
                </div>
            </div>
            <div class="contact-menu mt-2">
                <a href="/contacts/<%= contact._id %>/edit" class="btn btn-light mr-2">Edit</a>                         <!--a태그는 get만 가능..-->
                <form action="/contacts/<%= contact._id %>?_method=delete" method="post">   <!--delete는 form으로 처리, form도 post만 가능하기 때문에 methodoverride사용하여 delete 바꿔줬음-->
                    <a href="#" class="btn btn-light" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>   <!--submit으로 삭제-->
                </form>
            </div>
        </div>
    </body>
</html>